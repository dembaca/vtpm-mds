# Lab inventory for QEMU nested testing (not Proxmox).
# MACs must match the IMDS NIC created by scripts/qemu-lab/start-guest.sh
vms:
  - id: "100"
    name: mds-lab-guest
    macs:
      - "52:54:00:a1:b2:c3"
